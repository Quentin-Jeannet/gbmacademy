{% extends "base.html.twig" %}

{% block body %}
    {{form_start(form)}}
    <div class="p-5">
        <div class="px-4">
            <p>Dommage. Acceptez-vous que l’on reste en
                contact ?</p>
            <p><b>Pour rester en contact</b>, merci de cliquer ci-dessous.</p>
            {{form_widget(form.onNewsLetter)}}
        </div>
        <div class="d-flex flex-wrap justify-content-between py-5">
            <div class="col-12 col-lg-6 px-4">{{form_row(form.nom)}}</div>
            <div class="col-12 col-lg-6 px-4">{{form_row(form.prenom)}}</div>
        </div>
        <div class="d-flex flex-wrap">
            <div class="col-12 col-lg-6 px-4">{{form_row(form.email)}}</div>
        </div>
        <div class="d-flex justify-content-center pt-5">
            {{form_row(form.submit)}}
        </div>
    <p class="form-text text-muted">Champs obligatoire <sup class="text-danger">*</sup></p>
    </div>
    {{form_end(form)}}

{% endblock %}



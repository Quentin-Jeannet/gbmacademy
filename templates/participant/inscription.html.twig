{% extends "base-2.html.twig" %}
{% block body %}
<nav class="navbar navbar-expand-lg navbar-light bg-light p-3">
    <div class="container-fluid">
        <a class="navbar-brand p-4 w-30" href="#">
            <img src="{{asset('images/logo2.png')}}" alt="logo" class="img-fluid">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link  me-2" aria-current="page" href="{{path('app_home')}}">Programme</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active me-2" href="{{path('app_inscription')}}">Inscription</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
<img src="{{asset('images/banner-inscription.png')}}" alt="" class="img-fluid">
<div class="p-lg-5">
    {% for key, messages in app.flashes %}
    <div role="alert" class="alert alert-{{ key }} my-2">
        {% for message in messages %}
        {{ message }}
        {% endfor %}
    </div>
    {% endfor %}
    <div class="px-2">

        {{form_start(form)}}
        <div class="row py-3">
            <div>
                {{form_label(form.email)}}
                {{form_widget(form.email)}}
            </div>
        </div>
        <div class="row py-3">
            <div class="col-12 col-md-6">
                <div>
                    {{form_label(form.titre)}}
                    {{form_widget(form.titre)}}
                </div>
            </div>
            <div class="col-12 col-md-6">
                <div>
                    {{form_label(form.civilite)}}
                    {{form_widget(form.civilite)}}
                </div>
            </div>
        </div>
        <div class="row py-3">
            <div class="col-12 col-md-6">
                <div>
                    {{form_label(form.nom)}}
                    {{form_widget(form.nom)}}
                </div>
            </div>
            <div class="col-12 col-md-6">
                <div>
                    {{form_label(form.prenom)}}
                    {{form_widget(form.prenom)}}
                </div>
            </div>
        </div>
        <div class="row py-3">
            <div class="col-12 col-md-6">
                <div>
                    {{form_label(form.dateNaissance)}}
                    {{form_widget(form.dateNaissance)}}
                </div>
            </div>
            <div class="col-12 col-md-6">
                <div>
                    {{form_label(form.telephone)}}
                    {{form_widget(form.telephone)}}
                </div>
            </div>
        </div>
        <div class="row py-3">
            <div class="col-12 col-md-6">
                <div>
                    {{form_label(form.ville)}}
                    {{form_widget(form.ville)}}
                </div>
            </div>
            <div class="col-12 col-md-6">
                <div>
                    {{form_label(form.hopital)}}
                    {{form_widget(form.hopital)}}
                </div>
            </div>
        </div>
        <div class="row py-3">
            <div class="col-md-4">
                <div>
                    {{form_label(form.specialite)}}
                    {{form_widget(form.specialite)}}
                </div>
            </div>
            <div class="col-md-4">
                <div>
                    {{form_label(form.service)}}
                    {{form_widget(form.service)}}
                </div>
            </div>
            <div class="col-md-4">
                <div>
                    {{form_label(form.numRPPS)}}
                    {{form_widget(form.numRPPS)}}
                </div>
            </div>
        </div>

        <div class="text-center bg-primary">
            <i>Certificat de présence</i>
        </div>
        <div class="py-3">
            {{form_label(form.certificat)}}
            <div class="d-flex ms-2">
                {% for certificat in form.certificat %}
                {{form_widget(certificat)}}
                {% endfor %}
            </div>
        </div>

        <div class="text-center bg-primary">
            <i>Hébergement</i>
        </div>
        <div class="py-3">
            {{form_label(form.hebergement)}}
            <div class="d-flex ms-2">
                {% for hebergement in form.hebergement %}
                {{form_widget(hebergement)}}
                {% endfor %}
            </div>
        </div>

        <div class="text-center bg-primary">
            <i>Transport</i>
        </div>
        <div class="py-3">
            {{form_label(form.transport)}}
            <div class="d-flex ms-2">
                {% for transport in form.transport %}
                    {{form_widget(transport)}}
                {% endfor %}

            </div>
        </div>

        <div class="mt-5 pb-3">
            <p class="help">Les transferts en taxis ou VTC feront l’objet d’un remboursement sur présentation des justificatifs.</p>
            <p class="help">Tous les remboursements sont soumis à la loi applicable et à la politique de Novocure® en la matière. Veuillez contacter
                <a class="text-decoration-none text-color" href="mailto:dcandille@novocure.com">dcandille@novocure.com</a> si vous
                avez des doutes ou des questions.</p>
        </div>


        <div class="text-center bg-primary">
            Informations personnelles
        </div>

        <div class="py-3">
            {{form_widget(form.onNewsLetter)}}
        </div>

       



        <footer class="p-2 text-white py-4 my-3">
            <p class="mb-2">© 2023 Novocure GmbH, tous droits réservés.</p>
            <p>Novocure® et Novocollege® sont des marques commerciales de Novocure GmbH. </p>
            <p>FR-SRC-0032. Août 2023.</p>
            <p>Invitation à la 2ème Journée Nationale de la GBM Academy</p>
            <p>NOVOCURE FRANCE SAS 4, rue de Marivaux Paris 75002 France</p>
        </footer>

        <div class="-pmy-3">
            {{form_widget(form.conditions)}}
        </div>

        <div class="d-flex justify-content-center my-5">
            <button type="submit" class="btn btn-primary"><i class="fa-solid fa-pen"></i> ENVOYER</button>
        </div>


        {{form_end(form)}}

    </div>
    {% endblock %}